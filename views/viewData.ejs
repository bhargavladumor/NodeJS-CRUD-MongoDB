<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View</title>
</head>
<body>
    <table border="1">
        <tr>
            <td>Name</td>
            <td>Age</td>
            <td>Gender</td>
            <td>Hobby</td>
            <td>City</td>
            <td>Message</td>
            <td>Delete</td>
            <td>Update</td>
        </tr>
        <% for(var v of stData){ %>
            <tr>
                <td><%= v.name %></td>
                <td><%= v.age %></td>
                <td><%= v.gender %></td>
                <td><%= v.hobby %></td>
                <td><%= v.city %></td>
                <td><%= v.message %></td>
                <td><a href="/deleteData/<%= v.id %>">Delete</a></td>
                <td><a href="/updateData/<%= v.id %>">Update</a></td>
            </tr>
        <% } %>
    </table>
    <a href="/">Add</a>
</body>
</html>